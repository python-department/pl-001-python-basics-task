[project]
name = "generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Иван Курников", email = "iakurniovgmai@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
generator = "generator:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
